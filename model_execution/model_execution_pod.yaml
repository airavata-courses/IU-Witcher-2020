apiVersion: v1
kind: Pod
metadata:
  name: model-execution
  labels:
    name: model-execution
    app: todoapp

spec:
      containers:
      - image: iuwitcher2020/dockerized_model_execution
        name: model-execution
        ports:
        - name: model-execution
          containerPort: 5002

      #   volumeMounts:
      #     - name: data-retrieva
      #       mountPath: /data/db
      #
      # volumes:
      #     - name: data-retrieval-microservice
      #       hostPath:
      #         path: /data/db
