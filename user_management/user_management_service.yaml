apiVersion: v1
kind: Service
metadata:
  name: user-management
  labels:
    name: user-management
    app: todoapp

spec:
  selector:
    name: user-management

  type: NodePort
  ports:
    - name: user-management
      port: 5004
      targetPort: 5004
