apiVersion: v1
kind: Service
metadata:
  name: server
  # labels:
  #   name: server
  #   app: witcherapp

spec:
  selector:
    # name: server
    app: app
  type: LoadBalancer
  ports:
   # - name: http
   - port: 4321
     targetPort: 4321
     nodePort: 32171
     protocol: TCP
---
apiVersion: v1
kind: Pod

metadata:
  name: server
  labels:
    name: server
    app: witcherapp

spec:
  containers:
    - image: iuwitcher2020/dockerized_session_management:latest
      name: myserver
      ports:
        - containerPort: 4321